/**
 * EXAMPLE / TEMPLATE — safe to commit
 * Copy this file to maps_api_config.js and fill in your real API key.
 * The real maps_api_config.js is listed in .gitignore and must NOT be committed.
 */

window.GMAPS_CONFIG = {
  apiKey: 'YOUR_GOOGLE_MAPS_API_KEY_HERE',
  mapId: 'kerala-smarttransit-map',
  defaultCenter: { lat: 10.15, lng: 76.38 },
  defaultZoom: 8,
};
