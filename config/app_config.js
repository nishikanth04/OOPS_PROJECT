/**
 * Copy this file to app_config.js and add your own values there.
 * app_config.js is ignored by Git, so credentials are not committed.
 *
 * For Google Sign-In, add http://127.0.0.1:8080 (or your deployed site URL)
 * to the OAuth client's Authorized JavaScript origins in Google Cloud Console.
 */
window.SMARTTRANSIT_CONFIG = {
  geminiApiKey: "",
  googleClientId: "",
};
